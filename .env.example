# Set this to your Linear API key for headless / CI use.
# Get one at https://linear.app/settings/api
# Never commit your real .env.
LINEAR_API_KEY=lin_api_replace_me

# Optional: point at a non-production Linear API (e.g. the local emulator).
# LINEAR_API_URL=http://localhost:4100/graphql
